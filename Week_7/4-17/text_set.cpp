#include <iostream>
#include <set>
#include <vector>

using namespace std;

int main() {
	
	vector<int> vect;
	for(vector<int>::size_type ix = 0; ix != 10; ++ix) {
		vect.push_back(ix);
	}

	return 0;
}
